version: 2

models:
  - name: dim_user
    columns:
      - name: user_sk
        tests: [not_null, unique]

  - name: dim_plan
    columns:
      - name: plan_sk
        tests: [not_null, unique]

  - name: dim_date
    columns:
      - name: date_day
        tests: [not_null, unique]