version: 2

models:
  - name: fct_payments
    columns:
      - name: payment_id
        tests: [not_null, unique]

      - name: user_sk
        tests: [not_null]

      - name: plan_sk
        tests: [not_null]
  - name: fct_mrr
    columns:
      - name: user_sk
        tests: [not_null]

      - name: mrr_month
        tests: [not_null]      


  - name: fct_cohorts
    columns:
      - name: cohort_month
        tests: [not_null]

      - name: activity_month
        tests: [not_null]     